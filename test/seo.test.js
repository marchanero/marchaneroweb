describe('SEO', () => {
  it('debería tener una etiqueta de título', () => {
    // Esta es una prueba ficticia. Deberías reemplazarla con pruebas reales.
    document.title = 'Título de prueba';
    expect(document.title).toBe('Título de prueba');
  });
});